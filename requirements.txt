# Requer Python 3.12+ (necessário para compatibilidade com numpy recente,
# caso o projeto seja publicado no Streamlit Community Cloud — ver docs/especificacao.md).

pandas
numpy
requests
yfinance
beautifulsoup4
xlrd
streamlit
plotly
pytest
ruff

# openpyxl: sem uso em produção (o único adapter que lê Excel,
# ingest/gpr.py, baixa .xls e usa xlrd) — mas tests/test_gpr.py precisa
# dele pra fabricar bytes de Excel falsos via df.to_excel (motor padrão
# do pandas pra escrita de .xlsx). Confirmado removendo e rodando a suíte
# numa venv limpa: sem ele, 2 testes de gpr quebram com ModuleNotFoundError.
openpyxl
